# gb-524 无线频谱干扰三角定位

请生成英文短名为 `spectrum-interference-triangulation` 的 Go 全栈 Web 项目。系统供无线电频谱分析人员录入离线测向观测，计算多个方位线的交汇区域、评估残差并形成干扰源定位案例。系统不接收实时监听流、不控制无线设备、不自动执法或派工，所有坐标和结论只作决策支持并需人工复核。不得包含地图社交、工单、库存、订单、计费或通用监控功能。

## 项目主要需求

### 1. 核心实体

- `ReceiverStation`：字段至少含 `id`、`station_code`、`name`、`latitude`、`longitude`、`antenna_bias_deg`、`accuracy_deg`、`station_status`、`calibrated_at`；经纬度和误差范围必须校验，贯穿全栈。
- `BearingObservation`：字段至少含 `id`、`station_id`、`case_id`、`bearing_deg`、`signal_dbm`、`frequency_hz`、`bandwidth_hz`、`observed_at`、`quality`、`excluded_reason`；保存原始值和校正值，贯穿全栈。
- `InterferenceCase`：字段至少含 `id`、`case_code`、`frequency_center_hz`、`case_status`、`priority`、`opened_by`、`reviewer_id`、`conclusion`、`version`、`closed_at`；承载状态流和人工结论，贯穿全栈。
- `LocalizationEstimate`：字段至少含 `id`、`case_id`、`algorithm_version`、`latitude`、`longitude`、`uncertainty_radius_m`、`residual_deg`、`used_observation_ids_json`、`outlier_ids_json`、`estimate_status`、`created_at`；结果不可覆盖并可重放，贯穿全栈。

### 2. 核心页面

- `/stations` 测向站管理：消费 `ReceiverStation`、`BearingObservation`，查看校准与观测覆盖。
- `/observations` 观测工作台：消费 `BearingObservation`、`InterferenceCase`，录入、批量校验和排除低质量观测。
- `/localization` 三角定位页：消费 `LocalizationEstimate`、`BearingObservation`，在坐标平面展示方位线、估计点、残差和不确定区域。
- `/cases` 案例流转页：消费 `InterferenceCase`、`LocalizationEstimate`，提交分析、复核、确认或驳回。
- `/audit` 审计页：消费 `InterferenceCase`、`BearingObservation`，检索参数、排除动作和结论变更。

### 3. 差异算法与状态流

- 将 WGS84 小范围坐标转换到局部平面，构建测站方位射线；实现至少二站几何交点和三站以上加权最小二乘估计。
- 权重由测站精度和观测质量确定；输出角度残差、条件数或几何退化标志、不确定半径，平行或近共线方位不得返回伪精确点。
- 实现一次可解释的离群观测剔除：比较标准化残差，仅在剩余有效观测不少于 3 条时给出候选重算，并同时保存原估计与剔除证据。
- `InterferenceCase` 状态为 `draft -> collecting -> analyzing -> pending_review -> confirmed -> closed`；确认只能由 reviewer 执行，关闭后只读，使用乐观锁与事务阻止越级。

### 4. 横切关注点

- JWT/RBAC 角色 `observer`、`analyst`、`reviewer`、`admin`，权限必须贯穿数据库、后端中间件和路由、前端守卫与按钮显隐。
- 审计覆盖测站校准、观测录入/排除、算法参数、定位运行、状态和结论，保存 request ID 及前后摘要。
- 全局错误模型区分坐标无效、频率不匹配、几何退化、并发冲突和权限问题；前端全局处理并展示错误码。
- 请求追踪、结构化访问日志、panic recovery 和基于内存令牌桶的登录/定位限流独立实现。

### 5. 共享枚举、组件、hooks 与中间件

- `ObservationQuality = good | fair | poor | excluded`：后端 `backend/internal/constants/observation.go`，前端 `frontend/src/types/observation.ts`。
- `CaseStatus = draft | collecting | analyzing | pending_review | confirmed | closed`：后端 `backend/internal/constants/case.go`，前端 `frontend/src/types/case.ts`。
- README 必须列出枚举在数据库约束、后端各层和前端 store/component/page 的所有位置。
- 共享组件：`QualityBadge`（观测/定位页）、`BearingPlot`（测站/定位页）、`ReviewDecisionDialog`（案例/审计页）。
- hooks：`useAuth`、`useLocalizationRun`，集中处理权限、提交与轮询。
- 中间件至少有独立 `RequestID`、`AccessLog`、`Auth`、`RBAC`、`Recovery`、`RateLimit`。

### 6. 技术与规模红线

- React 18 + TypeScript + Vite + Material UI + Zustand + ECharts；坐标图使用本地笛卡尔视图，不依赖外部地图密钥。
- Go 1.22 + Gin + GORM + PostgreSQL，validator/v10、结构化日志、构造器注入、事务、`%w` 和优雅停机。
- 功能 Go 代码 2500-4200 行、24-42 个功能 `.go` 文件且低于 5000 行；排除测试、vendor、生成代码和前端。禁止空文件或复制代码凑量。
- `/api/v1`、`/healthz`、`/readyz`、统一响应/分页/错误码；坐标转换、最小二乘、退化检测和状态机必须有确定性单测。
- 禁止 TODO、placeholder、随机定位结果、handler 直连数据库和非事务状态变更。

### 7. 文件结构强制清单

```text
./
├── go.work                 # use ./backend
├── docker-compose.yml
├── .env
├── .env.example
├── .gitignore
├── README.md
├── runtime_smoke.json
├── backend/
│   ├── go.mod
│   ├── Dockerfile
│   ├── cmd/server/main.go
│   └── internal/{config,model,dto,repository,service,handler,router,middleware,constants,localization,util}/
├── frontend/
│   ├── Dockerfile
│   ├── nginx.conf
│   └── src/{api,stores,types,components/common,hooks,pages,router,utils}/
└── output/execution.md
```

每个实体分别拆 model/dto/repository/service/handler/router；坐标、矩阵求解、残差、离群检测分文件。严禁把职责合并进单一文件。

根 `runtime_smoke.json` 只能是启动 manifest，不得承载构建、API 或 Browser 证据。按下列内容创建，并实现 smoke 用 GORM SQLite、Compose 用 PostgreSQL 的配置切换：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20524",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "DB_AUTO_MIGRATE": "true",
    "JWT_SECRET": "runtime-smoke-gb524-jwt-secret-at-least-32-bytes"
  },
  "ready_url": "http://127.0.0.1:20524/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

### 8. 部署、README 与端口

- Compose 项目名 `spectrum-interference-triangulation`；前端 `18524`、后端 `19524`、PostgreSQL `57524`，容器端口 `80/8080/5432`。
- Compose 顶层 `name`、无 `version:`，容器名带 `${COMPOSE_PROJECT_NAME:-spectrum-interference-triangulation}` 前缀；数据库使用命名卷，数据库、后端和前端均配置 healthcheck 并按健康状态依赖，`docker compose ps` 中所有长驻服务最终必须为 healthy。
- `.env`/示例包含 Compose 名、端口、数据库变量、JWT、CORS、几何退化阈值和日志级别。Nginx `/api` 转发 `backend:8080` 并支持 SPA；中文目录一键部署。
- README 中文且 Docker 启动优先，包含复制 env、地址、测试账号、功能、技术栈、目录、变量、API、枚举位置、算法假设/公式、安全边界、开发方式、排错和 License。

### 9. 强制真实验收

1. 根目录运行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`；规模实测必须为 2500-4200 行、24-42 个功能 `.go` 文件且低于 5000 行；前端完成依赖安装、类型检查和 `npm run build` 生产构建。
2. 运行 Compose config、构建启动、ps 和 `/healthz`、`/readyz`，确认数据库/后端 healthy 且前端可访问。
3. 至少 8 项接口冒烟：登录、测站创建、案例创建、三条观测录入、定位运行、退化输入拒绝、离群重算、提交复核、确认和审计查询；真实结果只写入 `output/execution.md`，不得写入 `runtime_smoke.json`。
4. 只用 Codex 内置 Browser 验证登录、5 页面、方位图、定位运行、残差详情、复核与权限显隐；禁止外部 Chrome，截图文件一律保存到 `output/`，Browser 步骤与结论只写入 `output/execution.md`。
5. 执行 `docker compose down -v --remove-orphans` 并确认本项目无容器残留，不清理其他项目。
6. 项目目录独立执行 `git init`，设置仓库本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。先提交除 `output/` 验证证据外的实现文件并取得实现 commit hash，将 hash 写入 `output/execution.md`，再把 `output/execution.md` 与截图作为第二次报告提交；最终 `git status --short` 为空。
